/// @description sync image index
image_index = active

if clicked
    clicked --
    
    
if net_manager.menu_mode != "settings" or in_match()
{
    x = -100
    y = -100
} else
{
    x = xoff
    y = yoff
}

