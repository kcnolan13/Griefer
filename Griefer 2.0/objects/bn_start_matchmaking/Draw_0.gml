/// @description draw normal + popups
event_inherited()

/*
if (instance_position(cursor.x,cursor.y,id) and objVarRead(net_manager,"game_mode") = "no_mode") or objVarRead(net_manager,"game_mode") = label
{
    var ptxt = "Players Online"
    var pnums = real(objVarRead(net_manager,label+"_players_online"))
    if pnums = 1
        ptxt = "Player Online"
    draw_popup(x, y, game_mode_name(label), label_subtitle, "", string(pnums)+" "+ptxt, objVarRead(net_manager,label+"_color"), 1)
}*/

/* */
/*  */
